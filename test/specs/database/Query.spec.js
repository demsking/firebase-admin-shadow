'use strict'

const assert = require('assert')
const { Reference, extension } = require('../../../lib/database')

/* global describe it */

describe('Object', () => {
  beforeEach(() => {
    extension.clear()
  })
})
