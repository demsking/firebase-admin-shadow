'use strict'

const assert = require('assert')
const { extension } = require('../../../lib/database')

/* global describe it */

describe('Database Service', () => {
  beforeEach(() => {
    Reference.clear()
  })
})
